{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "alchemy dev",
    "deploy": "alchemy deploy",
    "destroy": "alchemy destroy"
  },
  "dependencies": {
    "hono": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/openapi": "catalog:",
    "@orpc/zod": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@dicecho-auth/api": "workspace:*",
    "@dicecho-auth/auth": "workspace:*",
    "@dicecho-auth/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "tsdown": "^0.16.5",
    "@dicecho-auth/config": "workspace:*",
    "alchemy": "^0.81.2",
    "wrangler": "^4.54.0",
    "@types/node": "catalog:",
    "@cloudflare/workers-types": "catalog:"
  }
}
